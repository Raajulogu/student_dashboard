import React from "react";
import data from "../Basic/data";

function Delete({students,setstudents,index}){
    

    return(
        <div>
            Delete
        </div>
    )
}

export default Delete;